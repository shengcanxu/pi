# Contributing

Run the full local check before opening changes:

```bash
npm install
npm run check
npm test
npm pack --dry-run
```

Do not commit credentials, user home config, or unsanitized QA transcripts.
